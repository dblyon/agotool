version: '3'


services:

  db: # name
    image: "postgres:9.6.5"
    volumes:
      - "dbdata:/var/lib/postgresql/data" # named volume, persistent data, lives in special directory from docker
      - ./data:/agotool_data # bind mounted volume from host, e.g. RW access from container.
#      - "agotool_data:/agotool_data"
    env_file:
      - env_file
    networks:
      - db_nw

  flaskapp:
    build: .
#    volumes:
#      - "agotool_data:/agotool_data"
    env_file:
      - env_file
    volumes:
      - .:/opt/services/flaskapp/src
    networks:
      - db_nw
      - web_nw
    depends_on:
      - db

  nginx:
    image: "nginx:1.13.5"
    ports:
      - "8080:80"
    volumes:
      - ./conf.d:/etc/nginx/conf.d
    networks:
      - web_nw
    depends_on:
      - flaskapp


networks:
  db_nw:
    driver: bridge
  web_nw:
    driver: bridge


volumes:
  dbdata:
  agotool_data:
